import TQI.Channels

/-!
# Measurements

This file tracks Chapter 2.3: measurements as collections of effects,
measurement probabilities, post-measurement states, ensembles, and extremal
measurements.

Use Physlib's existing measurement and ensemble API rather than defining local
copies:

* POVMs: `POVM` from `QuantumInfo.Measurements.POVM`;
* mixed-state ensembles: `MEnsemble` from `QuantumInfo.States.Ensemble`;
* pure-state ensembles: `PEnsemble` from `QuantumInfo.States.Ensemble`.
-/

namespace TQI

end TQI
